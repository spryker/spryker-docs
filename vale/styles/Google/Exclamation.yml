extends: existence
message: "Avoid using exclamation points per the Google Style Guide."
level: error
scope: sentence
nonword: true
tokens:
  - '!'
exceptions:
  - '<!--.*?-->'  # Ignore exclamation marks inside comments
  - '!['          # Ignore exclamation marks before square brackets (Markdown images/links)
