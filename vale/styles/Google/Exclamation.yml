extends: conditional
message: "Avoid using exclamation points per the Google Style Guide."
level: error
scope: sentence

# Define when the rule should be active (outside comments)
conditions:
  - pattern: "<!--.*?-->"
    scope: raw
    action: stop

  - pattern: "!"
    scope: raw
    action: error
